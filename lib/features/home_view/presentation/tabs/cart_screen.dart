import 'package:flutter/material.dart';

class CartScreen extends StatelessWidget {
  const CartScreen({super.key, required initialProduct});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
